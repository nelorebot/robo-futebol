name: Teste Robo

on:
  workflow_dispatch:

jobs:
  run-robo:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: python robo.py
